{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: commands of main.rs and daemon events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
